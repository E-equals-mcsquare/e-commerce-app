{
    "name": "GitHub Codespace with kubectl",
    "build": {
      "dockerfile": "Dockerfile"
    },
    "settings": {},
    "extensions": [
      "ms-kubernetes-tools.vscode-kubernetes-tools" // Optional: Install Kubernetes extension for VS Code
    ],
    "postCreateCommand": "kubectl version --client",
    "remoteUser": "vscode"
  }
  